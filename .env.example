# LLM 키 (둘 중 하나만 있으면 됨. 없으면 준비된 샘플 진정서로 폴백)
# Gemini (Google AI Studio) — AIza... 또는 AQ.... 로 시작
GEMINI_API_KEY=
# Claude (Anthropic) — sk-ant-... 로 시작
ANTHROPIC_API_KEY=

# 서버 포트 (기본 8080 — Flutter 앱이 localhost:8080 호출)
PORT=8080
